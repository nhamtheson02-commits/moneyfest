export const leadStatuses = ["new", "contacted", "qualified", "converted", "archived"] as const;

export const consultationStatuses = ["new", "contacted", "scheduled", "completed", "archived"] as const;

export const pageSizeOptions = [10, 20, 50] as const;

export const defaultPageSize = 10;
